# Result_Analysis
🎓 Student Progress Portal
A professional web application built with Python (Flask) that allows students to upload a CSV database and view their academic progress reports in a clean, high-fidelity interface. It includes features for real-time data parsing and automated PDF generation.

🛠️ Installation & Setup
1. Install Python
Ensure you have Python 3.8 or higher installed on your system.

2. Install Required Libraries
Open your terminal or command prompt and run:

Bash

pip install flask pandas reportlab

Flask: Powers the web server.

Pandas: Used for fast searching and parsing of the CSV database.

ReportLab: Handles the generation of the downloadable PDF report.

✨ Features
Smart Search: Automatically converts USN input to uppercase to match database records.

Dynamic Totals: Calculates the sum of all internal marks automatically.

Professional UI: Features a Midnight Blue theme inspired by institutional portals.

One-Click PDF: Generates a formal PDF document using a coordinate-based layout system.
